services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: after_rain_town
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - after-rain-data:/var/lib/postgresql/data

volumes:
  after-rain-data:
